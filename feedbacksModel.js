import modelExtend from 'dva-model-extend';
import { commonModel } from './commonModel';

export default modelExtend(commonModel, {
  namespace: 'feedbacksModel',
  state: {
  },
  effects: {
  },

});
